package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ===================================================
// PALETA iOS 26 — semántica de sistema, acento KAPTA
// ===================================================

// Acento KAPTA (marca) adaptado al azul sistema iOS
val KaptaAccentLight = Color(0xFF4F46E5)      // Índigo KAPTA (iOS blue-ish)
val KaptaAccentDark = Color(0xFF818CF8)       // Índigo claro para modo oscuro

// Fondo agrupado de iOS (grouped / insetGrouped)
val iOSSystemGroupedBackgroundLight = Color(0xFFF2F2F7)
val iOSSystemGroupedBackgroundDark = Color(0xFF000000)
val iOSSecondaryGroupedBackgroundLight = Color(0xFFFFFFFF)   // "Surface" de tarjetas (blanco)
val iOSSecondaryGroupedBackgroundDark = Color(0xFF1C1C1E)
val iOSTertiaryGroupedBackgroundLight = Color(0xFFF2F2F7)
val iOSTertiaryGroupedBackgroundDark = Color(0xFF2C2C2E)

// Separadores (hairline) con opacidad de sistema
val iOSSeparatorLight = Color(0x33000000) // 20% negro
val iOSSeparatorDark = Color(0x33FFFFFF)  // 20% blanco

// Texto
val iOSLabelLight = Color(0xFF000000)     // label primary
val iOSLabelDark = Color(0xFFFFFFFF)
val iOSSecondaryLabelLight = Color(0x99000000) // 60%
val iOSSecondaryLabelDark = Color(0x99FFFFFF)
val iOSTertiaryLabelLight = Color(0x4D000000)  // 30%
val iOSTertiaryLabelDark = Color(0x4DFFFFFF)

// Fill (fondo de chips/botones secundarios)
val iOSSystemFillLight = Color(0x0F000000) // ~6%
val iOSSystemFillDark = Color(0x1AFFFFFF)

// Celdas de lista iOS (separador hairline)
val iOSListBackgroundLight = Color(0xFFFFFFFF)
val iOSListBackgroundDark = Color(0xFF1C1C1E)
